# SBWAA — Variáveis de Ambiente
# Copiar para .env e configurar antes de usar
# NUNCA commitar .env (está no .gitignore)

ANTHROPIC_API_KEY=sua_chave_aqui

# TTL do cache por tipo de dado (em minutos)
# Descomente e ajuste para sobrescrever os padrões
# CACHE_TTL_COTACAO_MIN=15        # cotações em tempo quase-real (padrão: 15)
# CACHE_TTL_MACRO_MIN=60          # índices globais e macro (padrão: 60)
# CACHE_TTL_DIVIDENDOS_MIN=360    # dividendos e proventos (padrão: 360 = 6h)
# CACHE_TTL_FUNDAMENTALS_MIN=1440 # múltiplos e consenso (padrão: 1440 = 24h)
# CACHE_TTL_HISTORICO_MIN=1440    # séries OHLCV (padrão: 1440 = 24h)
# CACHE_TTL_BCB_MIN=1440          # Banco Central (padrão: 1440 = 24h)
# CACHE_TTL_MODELOS_MIN=1440      # DCF, quant, risco, econometria (padrão: 1440 = 24h)
